name: Release

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest

    env:
      HUSKY: 0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run semantic-release
        env:
          # A PAT, not the default GITHUB_TOKEN: pushes made with the default token don't
          # trigger other workflows (GitHub's anti-loop rule), so the "v*" tag this step
          # creates would silently never fire azure-deploy.yml's prod deployment.
          GITHUB_TOKEN: ${{ secrets.RELEASE_PAT }}
        run: pnpm exec semantic-release ${DRY_RUN:+--dry-run}
